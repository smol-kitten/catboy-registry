# X.509 extensions (arc .1.2)

ASN.1 module: `CatboyExtensions.asn1` (generated from `templates/x509/`, compiled and DER round-tripped in CI). Field set for `.1.2.1` mirrors Sigstore/Fulcio (issuer, repository, workflow ref, sha, run id, run attempt, trigger, runner name) so verifiers map 1:1.
