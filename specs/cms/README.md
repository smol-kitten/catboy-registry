# CMS signed attributes (arc .1.2.2)

Same run-metadata structure as the X.509 extension, carried as a signed attribute in detached `.p7s` signatures. ASN.1: `CatboySignedAttributes.asn1` (generated from `templates/cms/`, compiled and DER round-tripped in CI).
